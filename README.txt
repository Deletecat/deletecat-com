Website source files for deletecat.com.

To run it locally, you will need Ruby 3.x, then run these commands:
1. `bundle config set --local path vendor/bundle`
2. `bundle install`
3. `bundle exec jekyll serve`
or use `bundle exec jekyll build` to build the site.

₍^. .^₎⟆